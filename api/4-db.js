import{neon}from'@neondatabase/serverless';import seed from'./seed.js';export async function connect(){if(!process.env.DATABASE_URL)throw Error('database_not_configured');const sql=neon(process.env.DATABASE_URL);await sql`CREATE TABLE IF NOT EXISTS app_state(id text PRIMARY KEY,data jsonb NOT NULL,updated_at timestamptz DEFAULT now())`;await sql`INSERT INTO app_state(id,data) VALUES('main',${JSON.stringify(seed)}::jsonb) ON CONFLICT(id) DO NOTHING`;return sql}
